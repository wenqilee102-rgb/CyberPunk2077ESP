import class gameLightComponent extends entLightComponent
{
	var emissiveOnly : Bool;
	var materialZone : gameEMaterialZone;
	var meshBrokenAppearance : CName;
	var onStrength : Float;
	var turnOnByDefault : Bool;
	var turnOnTime : Float;
	var turnOnCurve : CName;
	var turnOffTime : Float;
	var turnOffCurve : CName;
	var loopTime : Float;
	var loopCurve : CName;
	var synchronizedLoop : Bool;
	var isDestructible : Bool;
	var colliderName : CName;
	var colliderTag : CName;
	var destructionEffect : raRef:worldEffect;
	var genericCurveSetOverride : rRef:CurveSet;

	static function ChangeAllLightsSettings(owner : whandle:gameObject, settings : ScriptLightSettings, optional time : Float, optional curve : CName, optional loop : Bool); // gameLightComponent::ChangeAllLightsSettings;GameObjectScriptLightSettingsFloatCNameBool
	static function ChangeLightSettingByRefs(lightRefs : script_ref:array:handle:gameLightComponent, setting : ScriptLightSettings, optional inTime : Float, optional interpolationCurve : CName, optional loop : Bool); // gameLightComponent::ChangeLightSettingByRefs;script_ref<array<gameLightComponent>>ScriptLightSettingsFloatCNameBool

	import function Destroy(optional forceDestroy : Bool, optional skipVFX : Bool); // Destroy
	import function GetCurrentSettings() : gameLightSettings; // GetCurrentSettings
	import function GetDefaultSettings() : gameLightSettings; // GetDefaultSettings
	import function GetOnStrength() : Bool; // GetOnStrength
	import function GetTurnOffCurve() : CName; // GetTurnOffCurve
	import function GetTurnOffTime() : Float; // GetTurnOffTime
	import function GetTurnOnCurve() : CName; // GetTurnOnCurve
	import function GetTurnOnTime() : Float; // GetTurnOnTime
	import function IsDestroyed() : Bool; // IsDestroyed
	import function IsDestructible() : Bool; // IsDestructible
	import function IsOn() : Bool; // IsOn
	import function SetAngles(innerAngle : Float, outerAngle : Float, optional inTime : Float); // SetAngles
	import function SetColor(color : Color, optional inTime : Float); // SetColor
	import function SetDestructible(isDestructible : Bool); // SetDestructible
	import function SetIntensity(intensity : Float, optional inTime : Float); // SetIntensity
	import function SetParameters(settings : gameLightSettings, optional inTime : Float, optional interpolationCurve : CName, optional loop : Bool); // SetParameters
	import function SetRadius(radius : Float, optional inTime : Float); // SetRadius
	import function SetStrength(strength : Float, optional inTime : Float); // SetStrength
	import function ToggleLight(on : Bool, optional loop : Bool); // ToggleLight

	event OnAdvanceChangeLight(evt : handle:AdvanceChangeLightEvent) : Bool; // OnAdvanceChangeLight
	event OnChangeCurveEvent(evt : handle:ChangeCurveEvent) : Bool; // OnChangeCurveEvent
	event OnChangeLight(evt : handle:ChangeLightEvent) : Bool; // OnChangeLight
	event OnChangeLightByName(evt : handle:ChangeLightByNameEvent) : Bool; // OnChangeLightByName
	event OnToggleLight(evt : handle:ToggleLightEvent) : Bool; // OnToggleLight
	event OnToggleLightByName(evt : handle:ToggleLightByNameEvent) : Bool; // OnToggleLightByName

	function SetupLightSettings(inputData : script_ref:EditableGameLightSettings, out outputData : gameLightSettings); // SetupLightSettings;script_ref<EditableGameLightSettings>gameLightSettings
}

import class entLightComponent extends entIVisualComponent
{
	var type : ELightType;
	var color : Color;
	var radius : Float;
	var unit : ELightUnit;
	var intensity : Float;
	var EV : Float;
	var temperature : Float;
	var lightChannel : rendLightChannel;
	var sceneDiffuse : Bool;
	var sceneSpecularScale : Uint8;
	var directional : Bool;
	var roughnessBias : Int8;
	var scaleGI : Uint8;
	var scaleEnvProbes : Uint8;
	var useInTransparents : Bool;
	var scaleVolFog : Uint8;
	var useInParticles : Bool;
	var attenuation : rendLightAttenuation;
	var clampAttenuation : Bool;
	var group : rendLightGroup;
	var areaShape : EAreaLightShape;
	var areaTwoSided : Bool;
	var spotCapsule : Bool;
	var sourceRadius : Float;
	var capsuleLength : Float;
	var areaRectSideA : Float;
	var areaRectSideB : Float;
	var innerAngle : Float;
	var outerAngle : Float;
	var softness : Float;
	var enableLocalShadows : Bool;
	var enableLocalShadowsForceStaticsOnly : Bool;
	var contactShadows : rendContactShadowReciever;
	var shadowAngle : Float;
	var shadowRadius : Float;
	var shadowFadeDistance : Float;
	var shadowFadeRange : Float;
	var shadowSoftnessMode : ELightShadowSoftnessMode;
	var rayTracedShadowsPlatform : rendRayTracedShadowsPlatform;
	var rayTracingLightSourceRadius : Float;
	var rayTracingContactShadowRange : Float;
	var iesProfile : raRef:CIESDataResource;
	var flicker : rendSLightFlickering;
	var envColorGroup : EEnvColorGroup;
	var colorGroupSaturation : Uint8;
	var portalAngleCutoff : Uint8;
	var allowDistantLight : Bool;
	var rayTracingIntensityScale : Float;
	var pathTracingLightUsage : rendEPathTracingLightUsage;
	var pathTracingOverrideScaleGI : Bool;
	var rtxdiShadowStartingDistance : Float;
	var isEnabled : Bool;

	import function SetColor(color : Color); // SetColor
	import function SetFlickerParams(strength : Float, period : Float, offset : Float); // SetFlickerParams
	import function SetIntensity(intensity : Float); // SetIntensity
	import function SetRadius(radius : Float); // SetRadius
	import function SetTemperature(temperature : Float); // SetTemperature

	event OnForceFlicker(evt : handle:FlickerEvent) : Bool; // OnForceFlicker
	event OnToggleLight(evt : handle:ToggleLightEvent) : Bool; // OnToggleLight
	event OnToggleLightByName(evt : handle:ToggleLightByNameEvent) : Bool; // OnToggleLightByName
}

import class abstract entIVisualComponent extends entIPlacedComponent
{
	var autoHideDistance : Float;
	var renderSceneLayerMask : RenderSceneLayerMask;
	var forceLODLevel : Int8;

	import function TemporaryHide(on : Bool); // TemporaryHide
}

import class abstract entIPlacedComponent extends entIComponent
{
	var localTransform : WorldTransform;
	var parentTransform : handle:entITransformBinding;

	import function GetInitialOrientation() : Quaternion; // GetInitialOrientation
	import function GetInitialPosition() : Vector4; // GetInitialPosition
	import function GetInitialTransform() : Matrix; // GetInitialTransform
	import function GetLocalOrientation() : Quaternion; // GetLocalOrientation
	import function GetLocalPosition() : Vector4; // GetLocalPosition
	import function GetLocalToWorld() : Matrix; // GetLocalToWorld
	import function GetLocalTransform() : Matrix; // GetLocalTransform
	import function GetTransformParent(); // GetTransformParent
	import function SetLocalOrientation(rot : Quaternion); // SetLocalOrientation
	import function SetLocalPosition(pos : Vector4); // SetLocalPosition
	import function SetLocalTransform(pos : Vector4, rot : Quaternion); // SetLocalTransform
}

import class abstract entIComponent extends IScriptable
{
	var name : CName;
	var isReplicable : Bool;
	var id : CRUID;

	import function FindComponentByName(componentName : CName) : whandle:entIComponent; // FindComponentByName
	import function GetAppearanceName() : CName; // GetAppearanceName
	import function GetEntity() : whandle:entEntity; // GetEntity
	import function GetName() : CName; // GetName
	import function IsEnabled() : Bool; // IsEnabled
	import function QueueEntityEvent(ev : handle:redEvent); // QueueEntityEvent
	import function RegisterRenderDebug(filterName : String, functionName : CName); // RegisterRenderDebug
	import function Toggle(on : Bool); // Toggle
}

import class IScriptable extends ISerializable
{
	import static function DetectScriptableCycles(); // DetectScriptableCycles

	import function GetClassName() : CName; // GetClassName
	import function IsA(className : CName) : Bool; // IsA
	import function IsExactlyA(className : CName) : Bool; // IsExactlyA
	import function ToString() : String; // ToString
}

import class abstract ISerializable
{
}