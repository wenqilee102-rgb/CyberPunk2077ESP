import class audioElevatorSettings extends audioEntitySettings
{
	var musicEvents : audioMusicController;
	var movementEvents : audioLoopingSoundController;
	var callingEvent : CName;
	var destinationReachedEvent : CName;
	var panelSelectionEvent : CName;
}

import class abstract audioEntitySettings extends audioAudioMetadata
{
	var commonSettings : audioCommonEntitySettings;
	var scanningSettings : audioScanningSettings;
	var auxiliaryMetadata : audioAuxiliaryMetadata;
	var emitterDecoratorMetadata : CName;
	var preferSoundComponentPosition : Bool;
}

import class abstract audioAudioMetadata extends audioAudioMetadataBase
{
}

import class abstract audioAudioMetadataBase extends ISerializable
{
	var name : CName;
}

import class abstract ISerializable
{
}