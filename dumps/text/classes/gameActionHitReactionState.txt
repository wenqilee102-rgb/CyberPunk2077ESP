import struct gameActionHitReactionState extends gameActionReplicatedState
{
	var animFeature : handle:animAnimFeature_HitReactionsData;
}

import struct gameActionReplicatedState
{
	var replicationId : Uint32;
	var type : Uint16;
	var startTimeStamp : netTime;
	var stopTimeStamp : netTime;
	var updateBucket : Uint8;
}