class CrosshairModule extends HUDModule
{
	private   var activeCrosshairs : array:handle:Crosshair;
}

class abstract HUDModule extends IScriptable
{
	protected var hud : whandle:HUDManager;
	protected var state : ModuleState;
	protected var instancesList : array:handle:ModuleInstance;

	function DuplicateLastInstance(actor : handle:gameHudActor) : handle:ModuleInstance; // DuplicateLastInstance;HUDActor
	function GetActiveMode() : ActiveMode; // GetActiveMode;
	function GetPlayer() : handle:gameObject; // GetPlayer;
	function GetState() : ModuleState; // GetState;
	function HasCurrentTarget() : Bool; // HasCurrentTarget;
	function InitializeModule(hud : whandle:HUDManager, state : ModuleState); // InitializeModule;HUDManagerModuleState
	function IsActorLookedAt(actor : handle:gameHudActor) : Bool; // IsActorLookedAt;HUDActor
	function IsActorLooted(actor : handle:gameHudActor) : Bool; // IsActorLooted;HUDActor
	function IsActorQuickHackTarget(actor : handle:gameHudActor) : Bool; // IsActorQuickHackTarget;HUDActor
	function IsIndexOK(index : Int32) : Bool; // IsIndexOK;Int32
	function IsModuleOperational() : Bool; // IsModuleOperational;
	function Iterate(out jobs : array:HUDJob, optional forcedMode : ActiveMode); // Iterate;array<HUDJob>ActiveMode
	function Iterate(out job : HUDJob, optional forcedMode : ActiveMode); // Iterate;HUDJobActiveMode
	function OverrideInstance(index : Int32, instance : handle:ModuleInstance); // OverrideInstance;Int32ModuleInstance
	function Process(out jobs : array:HUDJob, mode : ActiveMode); // Process;array<HUDJob>ActiveMode
	function Process(out task : HUDJob, mode : ActiveMode); // Process;HUDJobActiveMode
	function Suppress(jobs : script_ref:array:HUDJob); // Suppress;script_ref<array<HUDJob>>
	function UnregisterActor(actor : handle:gameHudActor); // UnregisterActor;HUDActor
}

import class IScriptable extends ISerializable
{
	import static function DetectScriptableCycles(); // DetectScriptableCycles

	import function GetClassName() : CName; // GetClassName
	import function IsA(className : CName) : Bool; // IsA
	import function IsExactlyA(className : CName) : Bool; // IsExactlyA
	import function ToString() : String; // ToString
}

import class abstract ISerializable
{
}